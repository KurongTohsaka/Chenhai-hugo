# v0.6.0 功能迭代 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- []) syntax for tracking.

**Goal:** 7 项功能迭代：相关文章推荐、代码块语言标签、Fuse.js 搜索、Series 系列、面包屑导航、构建耗时、Draft 预览

**Architecture:** 前端 3 项（CSS/JS）、后端 4 项（Go）。各任务独立，可并行实现。

**Tech Stack:** Go 1.23+, Goldmark, Go html/template, Fuse.js, CSS/JS

---

### Task 1: 相关文章推荐

**Files:**
- Modify: `internal/index/site.go` — 添加相关文章计算函数
- Modify: `internal/build/render.go` — 注入相关文章数据到页面模板
- Modify: `themes/zhenhai/layouts/single.html` — 相关文章推荐区域
- Modify: `themes/zhenhai/assets/css/style.css` — 推荐卡片样式

**实现思路：**

基于标签 Jaccard 相似度计算，为每篇文章推荐 3 篇最相关的文章。

- [ ] **Step 1: site.go 添加相关文章函数**

在 `internal/index/site.go` 末尾添加：

```go
// RelatedPosts returns up to n most related non-draft posts for the given page,
// based on tag overlap (Jaccard similarity).
func (s *Site) RelatedPosts(page *content.Page, n int) []*content.Page {
    if len(page.Tags) == 0 {
        return nil
    }
    type scored struct {
        p     *content.Page
        score float64
    }
    var candidates []scored
    pageTagSet := make(map[string]bool)
    for _, t := range page.Tags {
        pageTagSet[t] = true
    }
    for _, p := range s.PublishedPages() {
        if p == page || p.Permalink() == page.Permalink() {
            continue
        }
        intersection := 0
        for _, t := range p.Tags {
            if pageTagSet[t] {
                intersection++
            }
        }
        if intersection == 0 {
            continue
        }
        union := len(pageTagSet)
        for _, t := range p.Tags {
            if !pageTagSet[t] {
                union++
            }
        }
        score := float64(intersection) / float64(union)
        candidates = append(candidates, scored{p, score})
    }
    sort.Slice(candidates, func(i, j int) bool {
        return candidates[i].score > candidates[j].score
    })
    if len(candidates) > n {
        candidates = candidates[:n]
    }
    result := make([]*content.Page, len(candidates))
    for i, c := range candidates {
        result[i] = c.p
    }
    return result
}
```

- [ ] **Step 2: render.go 注入相关文章数据**

在 `internal/build/render.go` 的 `renderPages` 函数中，为每篇文章计算相关文章。在 `pageData` 的 `Extra` 中添加：

```go
related := site.RelatedPosts(page, 3)
if len(related) > 0 {
    pageData.Extra["related"] = related
}
```

- [ ] **Step 3: single.html 添加推荐区域**

在 `themes/zhenhai/layouts/single.html` 中，在现有 prev/next 导航下方（或文章内容下方）添加：

```html
{{if .Extra.related}}
<section class="related-posts">
  <h3 class="related-title">相关文章</h3>
  <div class="related-grid">
    {{range .Extra.related}}
    <a class="related-card" href="{{.Permalink}}">
      <span class="related-card-title">{{.Title}}</span>
      <span class="related-card-date">{{.Date.Format "2006-01-02"}}</span>
    </a>
    {{end}}
  </div>
</section>
{{end}}
```

- [ ] **Step 4: CSS 样式**

```css
.related-posts {
  margin-top: 3rem;
  padding-top: 1.5rem;
  border-top: 1px solid var(--color-border-light);
}
.related-title {
  font-size: 1.2rem;
  color: var(--color-heading);
  margin-bottom: 1rem;
}
.related-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 0.8rem;
}
.related-card {
  display: flex;
  flex-direction: column;
  padding: 1rem;
  border: 1px solid var(--color-border-light);
  border-radius: 6px;
  text-decoration: none;
  transition: border-color var(--transition-fast);
}
.related-card:hover {
  border-color: var(--color-link);
  text-decoration: none;
}
.related-card-title {
  font-weight: 600;
  color: var(--color-text);
  margin-bottom: 0.3rem;
}
.related-card-date {
  font-size: 0.82rem;
  color: var(--color-text-secondary);
}
```

- [ ] **Step 5: 测试 && Commit**

```bash
/usr/local/go/bin/go test ./internal/index/... ./internal/build/...
git add internal/index/site.go internal/build/render.go themes/zhenhai/layouts/single.html themes/zhenhai/assets/css/style.css
git commit -m "feat: add related posts based on tag Jaccard similarity"
```

---

### Task 2: 代码块语言标签

**Files:**
- Modify: `internal/content/highlight_lines.go` — 提取语言名并注入 data-lang
- Modify: `themes/zhenhai/assets/css/style.css` — 标签样式

**实现思路：**

扫描 markdown 中 fenced code block 的 info string，提取语言名，渲染后注入 `data-lang` 属性，CSS 伪元素显示。

- [ ] **Step 1: highlight_lines.go 添加语言提取**

在 `highlight_lines.go` 中添加：

```go
// fencedCodeRe matches ```lang or ```lang{...} and captures the language name.
var fencedCodeRe = regexp.MustCompile("`" + "`" + "`([a-zA-Z0-9_+#-]+)")

// extractLangs scans markdown source for fenced code block language names.
func extractLangs(source []byte) []string {
    var langs []string
    matches := fencedCodeRe.FindAllSubmatch(source, -1)
    for _, m := range matches {
        lang := strings.ToLower(string(m[1]))
        // Skip non-language markers like {hl_lines=...}
        if strings.HasPrefix(lang, "{") || lang == "" {
            continue
        }
        langs = append(langs, lang)
    }
    return langs
}

// injectLangLabels injects data-lang attributes into <pre class="chroma"> elements.
func injectLangLabels(html string, langs []string) string {
    if len(langs) == 0 {
        return html
    }
    preRe := regexp.MustCompile(`<pre[^>]*class="chroma"[^>]*>`)
    idx := 0
    return preRe.ReplaceAllStringFunc(html, func(match string) string {
        if idx < len(langs) {
            result := match[:len(match)-1] + fmt.Sprintf(` data-lang="%s">`, langs[idx])
            idx++
            return result
        }
        idx++
        return match
    })
}
```

修改 `RenderHTML` 管道为：
```
extractLangs → extractHLLines → md.Convert → injectHLLines → injectLangLabels → splitLineNumbers
```

修改 `RenderHTML`：
```go
func (r *Renderer) RenderHTML(source []byte) (string, error) {
    langs := extractLangs(source)
    cleaned, hlInfo := extractHLLines(source)
    var buf bytes.Buffer
    if err := r.md.Convert(cleaned, &buf); err != nil {
        return "", fmt.Errorf("render markdown: %w", err)
    }
    result := injectHLLines(buf.String(), hlInfo)
    result = injectLangLabels(result, langs)
    return splitLineNumbers(result), nil
}
```

同样修改 `RenderHTMLWithTOC`。

- [ ] **Step 2: CSS 语言标签**

在代码块 CSS 区域添加：

```css
/* Language label on code-wrapper */
.code-wrapper[data-lang]::before {
  content: attr(data-lang);
  position: absolute;
  top: 0;
  right: 3rem;
  padding: 0.15em 0.6em;
  font-family: var(--font-mono);
  font-size: 0.7rem;
  color: var(--color-text-secondary);
  background: var(--color-code-filename-bg);
  border-radius: 0 0 4px 4px;
  opacity: 0.7;
  z-index: 2;
}
```

注意：`data-lang` 属性需要从 `.code-body` 内的 `<pre class="chroma">` 转移到外层 `.code-wrapper`。修改 `splitLineNumbers` 使其传递 data-lang。

在 `splitLineNumbers` 中，提取 `data-lang` 属性并放到 wrapper 上：

```go
// In splitLineNumbers, after extracting attrs:
var langAttr string
if strings.Contains(attrs, "data-lang=") {
    re := regexp.MustCompile(`data-lang="([^"]*)"`)
    if m := re.FindStringSubmatch(attrs); len(m) > 1 {
        langAttr = fmt.Sprintf(` data-lang="%s"`, m[1])
        attrs = re.ReplaceAllString(attrs, "")
    }
}
// Then in the return:
return fmt.Sprintf(
    `<div class="code-wrapper"%s>...`, langAttr, ...
)
```

- [ ] **Step 3: 测试 && Commit**

```bash
/usr/local/go/bin/go test ./internal/content/...
git add internal/content/highlight_lines.go internal/content/renderer.go themes/zhenhai/assets/css/style.css
git commit -m "feat: add code block language label"
```

---

### Task 3: Fuse.js 搜索增强

**Files:**
- Modify: `themes/zhenhai/assets/js/search.js` — 替换搜索逻辑
- Modify: `themes/zhenhai/layouts/base.html` — 引入 Fuse.js CDN

**实现思路：**

用 Fuse.js 的模糊匹配替换当前的 `indexOf` 子串匹配，支持拼写容错。

- [ ] **Step 1: base.html 引入 Fuse.js CDN**

在 `</head>` 前添加：

```html
<script src="https://cdn.jsdelivr.net/npm/fuse.js@7.0.0/dist/fuse.min.js"></script>
```

- [ ] **Step 2: 重写 search.js 搜索核心**

替换 `scoreMatch` 和 `search` 函数为 Fuse.js 实现：

```js
  /* ================================================================ */
  /*  3. Fuse.js Fuzzy Search                                         */
  /* ================================================================ */

  var fuseOptions = {
    keys: [
      { name: "title", weight: 0.4 },
      { name: "summary", weight: 0.2 },
      { name: "content", weight: 0.1 },
      { name: "tags", weight: 0.2 },
      { name: "category", weight: 0.1 }
    ],
    threshold: 0.4,
    includeScore: true
  };

  var fuseIndex = null;

  function buildFuseIndex() {
    if (fuseIndex) return;
    if (searchIndex.length === 0) return;
    fuseIndex = new Fuse(searchIndex, fuseOptions);
  }

  function search(query) {
    if (!query || query.trim() === "") return [];
    if (!fuseIndex) buildFuseIndex();
    if (!fuseIndex) return [];

    var results = fuseIndex.search(query.trim());
    return results.map(function (r) { return r.item; });
  }
```

删除旧的 `scoreMatch` 函数和 `search` 函数（保留 `buildFuseIndex` 和新的 `search` 替代）。

保留 `loadSearchIndex`、`renderResults`、`performSearch`、`openSearch`、`closeSearch`、`navigateResults` 等函数不变。

- [ ] **Step 3: 测试 && Commit**

```bash
/usr/local/go/bin/go build -o /dev/null /Users/kurong/Project/GoProject/Chenhai-hugo/cmd/chenhai/
git add themes/zhenhai/assets/js/search.js themes/zhenhai/layouts/base.html
git commit -m "feat: replace substring search with Fuse.js fuzzy matching"
```

---

### Task 4: Series 系列文章

**Files:**
- Modify: `internal/content/page.go` — 添加 Series 字段
- Modify: `internal/content/frontmatter.go` — 解析 series 字段
- Modify: `internal/index/site.go` — 添加 Series 分组
- Modify: `internal/build/render.go` — 注入 Series 导航
- Modify: `themes/zhenhai/layouts/single.html` — Series 导航 UI
- Modify: `themes/zhenhai/assets/css/style.css` — 样式

- [ ] **Step 1: page.go 添加 Series 字段**

在 `Page` struct 中添加：
```go
Series string
```

- [ ] **Step 2: frontmatter.go 解析 Series**

在 `fmRaw` struct 和 `parseFM` 中添加 `Series string` 字段。

- [ ] **Step 3: site.go 添加 Series 分组**

```go
// Series holds pages grouped by series name.
Series map[string][]*content.Page
```

在 `BuildSite` 中 populate：
```go
if page.Series != "" {
    site.Series[page.Series] = append(site.Series[page.Series], page)
}
```

最后对每个 series 按日期排序：
```go
for _, pages := range site.Series {
    sort.Slice(pages, func(i, j int) bool {
        return pages[i].Date.Before(pages[j].Date)
    })
}
```

- [ ] **Step 4: render.go 注入 Series 导航**

```go
if page.Series != "" {
    if seriesPages, ok := site.Series[page.Series]; ok {
        for i, p := range seriesPages {
            if p.Permalink() == page.Permalink() {
                if i > 0 {
                    pageData.Extra["seriesPrev"] = seriesPages[i-1]
                }
                if i < len(seriesPages)-1 {
                    pageData.Extra["seriesNext"] = seriesPages[i+1]
                }
                break
            }
        }
    }
}
```

- [ ] **Step 5: single.html Series 导航**

```html
{{if or .Extra.seriesPrev .Extra.seriesNext}}
<nav class="series-nav">
  <span class="series-label">{{.Page.Series}} 系列</span>
  <div class="series-links">
    {{if .Extra.seriesPrev}}
    <a class="series-link prev" href="{{.Extra.seriesPrev.Permalink}}">← {{.Extra.seriesPrev.Title}}</a>
    {{end}}
    {{if .Extra.seriesNext}}
    <a class="series-link next" href="{{.Extra.seriesNext.Permalink}}">{{.Extra.seriesNext.Title}} →</a>
    {{end}}
  </div>
</nav>
{{end}}
```

- [ ] **Step 6: CSS**

```css
.series-nav {
  margin-top: 1.5rem;
  padding: 1rem;
  background: var(--color-bg-alt);
  border-radius: 6px;
  border-left: 3px solid var(--color-link);
}
.series-label {
  font-size: 0.82rem;
  color: var(--color-text-secondary);
  margin-bottom: 0.5rem;
  display: block;
}
.series-links {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
}
.series-link {
  font-weight: 600;
}
```

- [ ] **Step 7: 测试 && Commit**

```bash
/usr/local/go/bin/go test ./internal/...
git add internal/content/page.go internal/content/frontmatter.go internal/index/site.go internal/build/render.go themes/zhenhai/layouts/single.html themes/zhenhai/assets/css/style.css
git commit -m "feat: add series article navigation"
```

---

### Task 5: 面包屑导航

**Files:**
- Modify: `themes/zhenhai/layouts/list.html` — 在列表页顶部添加面包屑
- Create: `themes/zhenhai/layouts/partials/breadcrumb.html` — 面包屑 partial
- Modify: `themes/zhenhai/assets/css/style.css` — 样式

- [ ] **Step 1: breadcrumb.html partial**

```html
{{define "breadcrumb"}}
<nav class="breadcrumb" aria-label="Breadcrumb">
  <a href="/">首页</a>
  {{if .Extra.title}}
  <span class="breadcrumb-sep">›</span>
  <span class="breadcrumb-current">{{.Extra.title}}</span>
  {{end}}
</nav>
{{end}}
```

注意：Go 模板不允许 `{{define}}` 在已经定义了 `content` 的模板中。breadcrumb 应该在 list.html 的 content block 内直接渲染。

- [ ] **Step 2: list.html 添加面包屑**

在 `{{define "content"}}` 内的 `page-header` 前添加：

```html
<nav class="breadcrumb">
  <a href="/">首页</a>
  <span class="breadcrumb-sep">›</span>
  <span class="breadcrumb-current">{{.Extra.title}}</span>
</nav>
```

- [ ] **Step 3: CSS**

```css
.breadcrumb {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 1.2rem;
  font-size: 0.9rem;
  color: var(--color-text-secondary);
}
.breadcrumb a {
  color: var(--color-text-secondary);
}
.breadcrumb a:hover {
  color: var(--color-link);
}
.breadcrumb-sep {
  color: var(--color-border);
}
.breadcrumb-current {
  color: var(--color-heading);
  font-weight: 600;
}
```

- [ ] **Step 4: Commit**

```bash
git add themes/zhenhai/layouts/list.html themes/zhenhai/assets/css/style.css
git commit -m "feat: add breadcrumb navigation to list pages"
```

---

### Task 6: 构建耗时统计

**Files:**
- Modify: `internal/build/build.go` — 添加耗时记录

- [ ] **Step 1: build.go 添加耗时输出**

在 `Build()` 方法的每个关键步骤前后记录时间，最后输出汇总。添加 import `"time"`：

```go
func (b *Builder) Build() error {
    start := time.Now()
    fmt.Println("Chenhai 构建中...")

    // Step 1: Load cache
    t0 := time.Now()
    // ... existing cache loading code ...
    fmt.Printf("  ✓ 加载缓存 (%s)\n", time.Since(t0).Round(time.Millisecond))

    // Step 2: Collect pages
    t0 = time.Now()
    pages, err := b.collectPages()
    // ...
    fmt.Printf("  ✓ 扫描内容 (%s)\n", time.Since(t0).Round(time.Millisecond))

    // Step 3: Build site
    t0 = time.Now()
    site := index.BuildSite(b.cfg, pages)
    fmt.Printf("  ✓ 构建索引 (%s)\n", time.Since(t0).Round(time.Millisecond))

    // ... etc for each step ...

    fmt.Printf("\n✓ 构建完成 (%s)\n", time.Since(start).Round(time.Millisecond))
    return nil
}
```

在各阶段后输出耗时，格式统一为 `  ✓ <步骤名> (<耗时>)`。

- [ ] **Step 2: 测试 && Commit**

```bash
/usr/local/go/bin/go test ./internal/build/...
git add internal/build/build.go
git commit -m "feat: add build timing output for each phase"
```

---

### Task 7: Draft 预览模式

**Files:**
- Modify: `internal/cli/serve.go` — 添加 --drafts flag
- Modify: `internal/build/collect.go` — 使用 draft 配置
- Modify: `internal/build/builder.go` — Builder 增加 ShowDrafts 字段

- [ ] **Step 1: builder.go 添加 ShowDrafts**

```go
type Builder struct {
    cfg          *config.Config
    root         string
    renderer     *content.Renderer
    engine       *theme.Engine
    imageHost    *imagehost.Host
    cache        *BuildCache
    skippedPaths map[string]bool
    showDrafts   bool
}
```

修改 `New` 接受 `showDrafts bool`：

```go
func New(cfg *config.Config, root string, r *content.Renderer, e *theme.Engine, showDrafts bool) *Builder {
    return &Builder{
        cfg:        cfg,
        root:       root,
        renderer:   r,
        engine:     e,
        imageHost:  imagehost.New(&cfg.ImageHost),
        showDrafts: showDrafts,
    }
}
```

注意需要更新所有调用 `build.New(...)` 的地方（build.go CLI、server.go、builder_test.go）添加最后一个参数。

- [ ] **Step 2: collect.go 使用 showDrafts**

在 `collectPages` 中，draft 过滤逻辑改为使用 `b.showDrafts`。当前逻辑是 `if page.Draft { continue }`，需改为：

```go
// In BuildSite:
if page.Draft && !b.showDrafts {
    continue
}
```

同时 `PublishedPages` 也需要感知 showDrafts。或者直接在 Site 层面处理。

简化方案：在 `collectPages` 中始终收集所有页面（包括 draft），在 `BuildSite` 中过滤。但在 renderPages 中也跳过 draft。最低侵入性方案：在 serve 命令传 --drafts 时，Builder 构建 site 时不排除 draft。

具体实现：修改 `index.BuildSite` 接受 `showDrafts bool`，在过滤 draft 时检查：

```go
func BuildSite(cfg *config.Config, pages []*content.Page, showDrafts bool) *Site {
    // ...
    for _, page := range pages {
        if (page.Draft || page.Layout != "") && !showDrafts {
            continue
        }
        // ... rest
    }
}
```

需要同步更新所有 `BuildSite` 调用处（build.go 和所有测试）。

- [ ] **Step 3: serve.go 添加 --drafts flag**

```go
var drafts bool

func init() {
    serveCmd.Flags().IntVarP(&port, "port", "p", 1313, "服务器端口")
    serveCmd.Flags().BoolVarP(&drafts, "drafts", "D", false, "预览草稿")
}
```

修改 serve 的 `RunE`，传递 `drafts` 给 `server.New`。

修改 `server.New` 接受 `showDrafts bool`，传递给 `build.New`。

- [ ] **Step 4: 更新测试**

```bash
/usr/local/go/bin/go test ./internal/...
```

修复所有 `BuildSite(cfg, pages)` 调用，加上 `false` 参数。

- [ ] **Step 5: Commit**

```bash
git add internal/build/builder.go internal/build/collect.go internal/build/build.go internal/index/site.go internal/cli/serve.go internal/server/server.go
git commit -m "feat: add draft preview mode (chenhai serve --drafts)"
```

---

### Task 8: 版本号更新 + 文档

**Files:**
- Modify: `internal/cli/version.go` — v0.6.0
- Modify: `docs/planning.md` — 更新当前版本
- Modify: `README.md` — 更新版本号
- Modify: `themes/zhenhai/layouts/base.html` — CSS v=13

- [ ] **Step 1: 版本号**

```go
cmd.Println("chenhai v0.6.0")
```

- [ ] **Step 2: 文档更新**

更新 planning.md 当前版本为 v0.6.0，将 v0.6.0 条目移到版本历史。

- [ ] **Step 3: Commit && Tag**

```bash
git add internal/cli/version.go docs/planning.md README.md themes/zhenhai/layouts/base.html
git commit -m "chore: bump to v0.6.0"
git tag -a v0.6.0 -m "v0.6.0: 相关文章、代码语言标签、Fuse.js、Series、面包屑、构建耗时、Draft预览"
git push origin main --follow-tags
```
