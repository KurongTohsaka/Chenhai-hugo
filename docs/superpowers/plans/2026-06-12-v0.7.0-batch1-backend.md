# v0.7.0 第一批：后端基础 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现并发页面渲染、模板预编译缓存、chenhai doctor 命令、cover/pinned front matter 字段。

**Architecture:** 在现有 Build Pipeline 中引入 goroutine pool 并发渲染页面；在 Builder 中缓存 Engine 实例跨增量构建复用；新增 doctor 命令独立检测站点健康；扩展 Page 模型增加 cover/pinned 数据通道。

**Tech Stack:** Go 1.24, html/template, spf13/cobra, gopkg.in/yaml.v3

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `internal/content/page.go` | 修改 | 新增 Cover、Pinned 字段 |
| `internal/content/frontmatter.go` | 修改 | 解析 cover、pinned YAML 字段 |
| `internal/content/frontmatter_test.go` | 修改 | 测试新字段解析 |
| `internal/index/site.go` | 修改 | 排序逻辑 pinned 优先 |
| `internal/index/site_test.go` | 修改 | 测试 pinned 排序 |
| `internal/build/render.go` | 修改 | 并发渲染页面 |
| `internal/build/builder.go` | 修改 | 缓存 Engine、新增字段 |
| `internal/theme/engine.go` | 修改 | 模板源文件缓存 |
| `internal/cli/doctor.go` | 新建 | chenhai doctor 命令 |
| `internal/cli/root.go` | 修改 | 注册 doctor 命令 |
| `internal/cli/doctor_test.go` | 新建 | doctor 命令测试 |
| `internal/theme/engine_test.go` | 修改 | 模板源缓存测试 |

---

### Task 1: E4 + E6 — Front Matter 字段扩展（cover + pinned）

**Files:**
- Modify: `internal/content/page.go`
- Modify: `internal/content/frontmatter.go`
- Modify: `internal/content/frontmatter_test.go`

- [ ] **Step 1: 在 Page 结构体中新增 Cover 和 Pinned 字段**

编辑 `internal/content/page.go`，在 `AIGenerated bool` 之后添加：

```go
Cover  string
Pinned bool
```

完整改动后的 Page 结构体尾部：

```go
	FilePath string
	RelPath  string
	Section  string
	Series     string
	AIGenerated bool
	Cover       string
	Pinned      bool
}
```

- [ ] **Step 2: 在 fmRaw 中新增对应字段并更新 validKeys**

编辑 `internal/content/frontmatter.go`，在 `fmRaw` 的 `AIGenerated` 之后添加：

```go
Cover  string
Pinned bool
```

在 `validKeys` map 中添加两个新 key，在 `"ai_generated": true,` 之后：

```go
"cover": true, "pinned": true,
```

- [ ] **Step 3: 在 parseFM 中赋值新字段**

编辑 `internal/content/frontmatter.go`，在 `page.AIGenerated = raw.AIGenerated` 之后添加：

```go
page.Cover = raw.Cover
page.Pinned = raw.Pinned
```

- [ ] **Step 4: 运行已有测试确保无回归**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/content/ -v
```

预期：全部 PASS

- [ ] **Step 5: 编写新字段的解析测试**

编辑 `internal/content/frontmatter_test.go`，在文件末尾添加：

```go
func TestParseFrontMatter_CoverAndPinned(t *testing.T) {
	raw := `---
title: Featured Post
date: 2024-06-01
cover: https://example.com/cover.jpg
pinned: true
---
Body content.
`
	page, body, err := content.ParseFrontMatter([]byte(raw))
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}

	if page.Cover != "https://example.com/cover.jpg" {
		t.Errorf("Cover = %q, want %q", page.Cover, "https://example.com/cover.jpg")
	}
	if !page.Pinned {
		t.Error("Pinned should be true")
	}
	if string(body) != "Body content." {
		t.Errorf("body = %q, want %q", string(body), "Body content.")
	}
}

func TestParseFrontMatter_CoverAndPinned_Default(t *testing.T) {
	raw := `---
title: Normal Post
date: 2024-06-01
---
Body.
`
	page, _, err := content.ParseFrontMatter([]byte(raw))
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}

	if page.Cover != "" {
		t.Errorf("Cover should be empty by default, got %q", page.Cover)
	}
	if page.Pinned {
		t.Error("Pinned should be false by default")
	}
}
```

- [ ] **Step 6: 运行测试验证**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/content/ -v -run "CoverAndPinned"
```

预期：FAIL（如果还没实现）或 PASS（已实现）

- [ ] **Step 7: Commit**

```bash
git add internal/content/page.go internal/content/frontmatter.go internal/content/frontmatter_test.go
git commit -m "feat: add cover and pinned front matter fields"
```

---

### Task 2: E6 — Pinned 排序逻辑

**Files:**
- Modify: `internal/index/site.go`
- Modify: `internal/index/site_test.go`

- [ ] **Step 1: 修改 BuildSite 排序逻辑**

编辑 `internal/index/site.go`，将 `BuildSite` 中的排序部分：

```go
	sort.Slice(pages, func(i, j int) bool {
		return pages[i].Date.After(pages[j].Date)
	})
```

替换为：

```go
	sort.Slice(pages, func(i, j int) bool {
		if pages[i].Pinned != pages[j].Pinned {
			return pages[i].Pinned
		}
		return pages[i].Date.After(pages[j].Date)
	})
```

- [ ] **Step 2: 运行已有测试确保无回归**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/index/ -v
```

预期：全部 PASS（因为现有测试中没有 pinned 页面，行为不变）

- [ ] **Step 3: 编写 pinned 排序测试**

编辑 `internal/index/site_test.go`，在文件末尾添加：

```go
func TestBuildSite_PinnedSortOrder(t *testing.T) {
	cfg := config.DefaultConfig()
	pages := []*content.Page{
		{Title: "old-unpinned", Date: time.Date(2020, 1, 1, 0, 0, 0, 0, time.UTC), Pinned: false, RawContent: "x", RelPath: "posts/a.md", Section: "posts"},
		{Title: "new-unpinned", Date: time.Date(2025, 1, 1, 0, 0, 0, 0, time.UTC), Pinned: false, RawContent: "x", RelPath: "posts/b.md", Section: "posts"},
		{Title: "old-pinned", Date: time.Date(2020, 1, 1, 0, 0, 0, 0, time.UTC), Pinned: true, RawContent: "x", RelPath: "posts/c.md", Section: "posts"},
		{Title: "new-pinned", Date: time.Date(2025, 1, 1, 0, 0, 0, 0, time.UTC), Pinned: true, RawContent: "x", RelPath: "posts/d.md", Section: "posts"},
	}

	site := index.BuildSite(cfg, pages, false)

	if len(site.Pages) != 4 {
		t.Fatalf("expected 4 pages, got %d", len(site.Pages))
	}

	// Pinned pages should come first, sorted by date descending
	if site.Pages[0].Title != "new-pinned" {
		t.Errorf("page 0: expected 'new-pinned', got %q", site.Pages[0].Title)
	}
	if site.Pages[1].Title != "old-pinned" {
		t.Errorf("page 1: expected 'old-pinned', got %q", site.Pages[1].Title)
	}
	if site.Pages[2].Title != "new-unpinned" {
		t.Errorf("page 2: expected 'new-unpinned', got %q", site.Pages[2].Title)
	}
	if site.Pages[3].Title != "old-unpinned" {
		t.Errorf("page 3: expected 'old-unpinned', got %q", site.Pages[3].Title)
	}
}

func TestBuildSite_PinnedNoPinned(t *testing.T) {
	cfg := config.DefaultConfig()
	pages := []*content.Page{
		{Title: "newest", Date: time.Date(2025, 1, 1, 0, 0, 0, 0, time.UTC), Pinned: false, RawContent: "x", RelPath: "posts/a.md", Section: "posts"},
		{Title: "oldest", Date: time.Date(2020, 1, 1, 0, 0, 0, 0, time.UTC), Pinned: false, RawContent: "x", RelPath: "posts/b.md", Section: "posts"},
	}

	site := index.BuildSite(cfg, pages, false)

	if len(site.Pages) != 2 {
		t.Fatalf("expected 2 pages, got %d", len(site.Pages))
	}
	// No pinned pages, should sort by date descending as before
	if site.Pages[0].Title != "newest" {
		t.Errorf("page 0: expected 'newest', got %q", site.Pages[0].Title)
	}
	if site.Pages[1].Title != "oldest" {
		t.Errorf("page 1: expected 'oldest', got %q", site.Pages[1].Title)
	}
}
```

- [ ] **Step 4: 运行新测试验证**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/index/ -v -run "Pinned"
```

预期：全部 PASS

- [ ] **Step 5: Commit**

```bash
git add internal/index/site.go internal/index/site_test.go
git commit -m "feat: pinned pages sort first in site index"
```

---

### Task 3: P1 — 并发页面渲染

**Files:**
- Modify: `internal/build/render.go`

- [ ] **Step 1: 重构 renderPages 为并发版本**

编辑 `internal/build/render.go`，在 import 中添加：

```go
import (
	"fmt"
	"path/filepath"
	"runtime"
	"strings"
	"sync"

	"github.com/KurongTohsaka/chenhai-hugo/internal/index"
	"github.com/KurongTohsaka/chenhai-hugo/internal/theme"
)
```

将 `renderPages` 函数体替换为：

```go
func (b *Builder) renderPages(site *index.Site, public string) error {
	// Render paginated homepage (always rendered — it may include changed pages)
	published := site.PublishedPages()
	if err := b.renderPaginatedListPages(b.cfg.Title, published, public, "index.html", site, public); err != nil {
		return fmt.Errorf("homepage: %w", err)
	}

	// Collect non-draft, non-skipped pages to render
	type task struct {
		page     *content.Page
		pageData *theme.TemplateData
		outDir   string
		tmpl     string
	}
	var tasks []task
	for _, page := range site.Pages {
		if page.Draft {
			continue
		}
		if b.skippedPaths[page.FilePath] {
			continue
		}

		permalink := page.Permalink()
		outDir := filepath.Join(public, strings.Trim(permalink, "/"))
		pageData := &theme.TemplateData{
			Site:   site,
			Page:   page,
			Config: b.cfg,
			Extra: map[string]interface{}{
				"hasMath":    strings.Contains(page.Content, "$$") || strings.Contains(page.Content, "\\("),
				"hasMermaid": strings.Contains(page.Content, "mermaid"),
			},
		}
		if related := site.RelatedPosts(page, 3); len(related) > 0 {
			pageData.Extra["related"] = related
		}
		if page.Series != "" {
			if seriesPages, ok := site.Series[page.Series]; ok {
				pageData.Extra["seriesPages"] = seriesPages
				pageData.Extra["seriesName"] = page.Series
				for i, p := range seriesPages {
					if p.FilePath == page.FilePath {
						pageData.Extra["seriesIdx"] = i
						if i > 0 {
							pageData.Extra["seriesPrev"] = seriesPages[i-1]
						}
						if i < len(seriesPages)-1 {
							pageData.Extra["seriesNext"] = seriesPages[i+1]
						}
						break
					}
				}
			}
		}
		tmpl := "single.html"
		if page.Layout != "" {
			tmpl = page.Layout + ".html"
		}
		tasks = append(tasks, task{page: page, pageData: pageData, outDir: outDir, tmpl: tmpl})
	}

	if len(tasks) == 0 {
		return nil
	}

	// Concurrent rendering with semaphore
	sem := make(chan struct{}, runtime.NumCPU())
	var wg sync.WaitGroup
	var mu sync.Mutex
	var firstErr error
	rendered := 0

	for _, tsk := range tasks {
		wg.Add(1)
		go func(t task) {
			defer wg.Done()
			sem <- struct{}{}
			defer func() { <-sem }()

			err := b.renderToFile(t.pageData, filepath.Join(t.outDir, "index.html"), t.tmpl)
			mu.Lock()
			if err != nil && firstErr == nil {
				firstErr = fmt.Errorf("page %q: %w", t.page.Title, err)
			}
			rendered++
			fmt.Printf("\r  渲染 %d/%d 篇文章", rendered, len(tasks))
			mu.Unlock()
		}(tsk)
	}
	wg.Wait()

	fmt.Println()
	if firstErr != nil {
		return firstErr
	}
	return nil
}
```

注意：需要添加 `"github.com/KurongTohsaka/chenhai-hugo/internal/content"` 到 import（如果 `task` 结构体使用了 `*content.Page`）。检查现有 import 中已有 `content` 的间接引用，但此处需要显式 import。实际上 `*theme.TemplateData` 的 `Page` 字段是 `*content.Page`，在 task 结构体中可以直接用 `*content.Page` 类型——但这需要 import content 包。为了避免循环依赖或多余的 import，直接将 task.page 类型改为接口或使用具体类型。简化方案：不定义 task 结构体，直接在闭包中捕获循环变量。

重写为更简洁的闭包版本：

```go
func (b *Builder) renderPages(site *index.Site, public string) error {
	// Render paginated homepage (always rendered — it may include changed pages)
	published := site.PublishedPages()
	if err := b.renderPaginatedListPages(b.cfg.Title, published, public, "index.html", site, public); err != nil {
		return fmt.Errorf("homepage: %w", err)
	}

	// Collect non-draft, non-skipped pages
	type renderTask struct {
		pageData *theme.TemplateData
		outDir   string
		tmpl     string
		title    string
	}
	var tasks []renderTask
	for _, page := range site.Pages {
		if page.Draft {
			continue
		}
		if b.skippedPaths[page.FilePath] {
			continue
		}

		permalink := page.Permalink()
		outDir := filepath.Join(public, strings.Trim(permalink, "/"))
		pageData := &theme.TemplateData{
			Site:   site,
			Page:   page,
			Config: b.cfg,
			Extra: map[string]interface{}{
				"hasMath":    strings.Contains(page.Content, "$$") || strings.Contains(page.Content, "\\("),
				"hasMermaid": strings.Contains(page.Content, "mermaid"),
			},
		}
		if related := site.RelatedPosts(page, 3); len(related) > 0 {
			pageData.Extra["related"] = related
		}
		if page.Series != "" {
			if seriesPages, ok := site.Series[page.Series]; ok {
				pageData.Extra["seriesPages"] = seriesPages
				pageData.Extra["seriesName"] = page.Series
				for i, p := range seriesPages {
					if p.FilePath == page.FilePath {
						pageData.Extra["seriesIdx"] = i
						if i > 0 {
							pageData.Extra["seriesPrev"] = seriesPages[i-1]
						}
						if i < len(seriesPages)-1 {
							pageData.Extra["seriesNext"] = seriesPages[i+1]
						}
						break
					}
				}
			}
		}
		tmpl := "single.html"
		if page.Layout != "" {
			tmpl = page.Layout + ".html"
		}
		tasks = append(tasks, renderTask{
			pageData: pageData,
			outDir:   outDir,
			tmpl:     tmpl,
			title:    page.Title,
		})
	}

	if len(tasks) == 0 {
		return nil
	}

	// Concurrent rendering with semaphore
	sem := make(chan struct{}, runtime.NumCPU())
	var wg sync.WaitGroup
	var mu sync.Mutex
	var firstErr error
	rendered := 0

	for _, tsk := range tasks {
		wg.Add(1)
		go func(t renderTask) {
			defer wg.Done()
			sem <- struct{}{}
			defer func() { <-sem }()

			err := b.renderToFile(t.pageData, filepath.Join(t.outDir, "index.html"), t.tmpl)
			mu.Lock()
			if err != nil && firstErr == nil {
				firstErr = fmt.Errorf("page %q: %w", t.title, err)
			}
			rendered++
			fmt.Printf("\r  渲染 %d/%d 篇文章", rendered, len(tasks))
			mu.Unlock()
		}(tsk)
	}
	wg.Wait()

	fmt.Println()
	if firstErr != nil {
		return firstErr
	}
	return nil
}
```

- [ ] **Step 2: 运行已有构建测试确保无回归**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/build/ -v
```

预期：全部 PASS

- [ ] **Step 3: 在本地博客站点验证构建**

```bash
cd /Users/kurong/Project/hekurong-site && ../Chenhai-hugo/chenhai build
```

预期：构建成功，渲染阶段输出 `渲染 N/M 篇文章` 进度，总耗时较之前下降。

- [ ] **Step 4: Commit**

```bash
git add internal/build/render.go
git commit -m "perf: concurrent page rendering with goroutine pool"
```

---

### Task 4: P2 — 模板预编译缓存

**Files:**
- Modify: `internal/build/builder.go`
- Modify: `internal/theme/engine.go`

- [ ] **Step 1: Engine 新增 templateSrcs 缓存字段**

编辑 `internal/theme/engine.go`，在 Engine 结构体中添加：

```go
type Engine struct {
	templates          *template.Template
	siteRoot           string
	extThemeLayoutsDir string
	templateSrcs       map[string][]byte // template source cache, keyed by name
}
```

- [ ] **Step 2: Engine 暴露设置缓存的方法**

编辑 `internal/theme/engine.go`，在 Engine 结构体定义之后添加：

```go
// SetTemplateSrcs sets the template source cache for reuse across renders.
func (e *Engine) SetTemplateSrcs(srcs map[string][]byte) {
	e.templateSrcs = srcs
}

// TemplateSrcs returns the template source cache.
func (e *Engine) TemplateSrcs() map[string][]byte {
	return e.templateSrcs
}
```

- [ ] **Step 3: readLayoutSource 优先从缓存读取**

编辑 `internal/theme/engine.go`，修改 `readLayoutSource` 方法，在函数体开头添加缓存检查。将：

```go
func (e *Engine) readLayoutSource(name string) ([]byte, error) {
	// Check site-specific layouts directory
	if e.siteRoot != "" {
```

改为：

```go
func (e *Engine) readLayoutSource(name string) ([]byte, error) {
	// Check in-memory cache first
	if e.templateSrcs != nil {
		if src, ok := e.templateSrcs[name]; ok {
			return src, nil
		}
	}

	// Check site-specific layouts directory
	if e.siteRoot != "" {
```

并在每个读取成功的位置将结果存入缓存。在 `readLayoutSource` 的返回语句之前统一包装：

完整替换后的 `readLayoutSource`：

```go
func (e *Engine) readLayoutSource(name string) ([]byte, error) {
	// Check in-memory cache first
	if e.templateSrcs != nil {
		if src, ok := e.templateSrcs[name]; ok {
			return src, nil
		}
	}

	var src []byte
	var err error

	// Check site-specific layouts directory
	if e.siteRoot != "" {
		siteLayoutPath := filepath.Join(e.siteRoot, "layouts", name)
		if info, statErr := os.Stat(siteLayoutPath); statErr == nil && !info.IsDir() {
			src, err = os.ReadFile(siteLayoutPath)
			if err == nil {
				if e.templateSrcs == nil {
					e.templateSrcs = make(map[string][]byte)
				}
				e.templateSrcs[name] = src
				return src, nil
			}
		}
	}

	// Check external theme layouts directory
	if e.extThemeLayoutsDir != "" {
		extPath := filepath.Join(e.extThemeLayoutsDir, name)
		if info, statErr := os.Stat(extPath); statErr == nil && !info.IsDir() {
			src, err = os.ReadFile(extPath)
			if err == nil {
				if e.templateSrcs == nil {
					e.templateSrcs = make(map[string][]byte)
				}
				e.templateSrcs[name] = src
				return src, nil
			}
		}
	}

	// Fall back to embedded theme
	src, err = zhenhai.FS.ReadFile("layouts/" + name)
	if err == nil {
		if e.templateSrcs == nil {
			e.templateSrcs = make(map[string][]byte)
		}
		e.templateSrcs[name] = src
	}
	return src, err
}
```

- [ ] **Step 4: Builder 缓存 Engine 实例**

编辑 `internal/build/builder.go`，在 Builder 结构体中添加字段：

```go
type Builder struct {
	cfg          *config.Config
	root         string
	renderer     *content.Renderer
	engine       *theme.Engine
	imageHost    *imagehost.Host
	showDrafts   bool
	cache        *BuildCache
	skippedPaths map[string]bool

	// P2: cached engine for incremental build reuse
	cachedEngine *theme.Engine
}
```

- [ ] **Step 5: 修改 Build() 复用缓存的 Engine**

编辑 `internal/build/builder.go`，在 `Build()` 方法中，找到创建 Engine 的位置（目前在调用方创建并传入 New）。检查发现 Builder 的 `engine` 字段是通过 `New()` 构造函数传入的，Engine 由外部创建。

需要修改策略：让 Builder 在构建时决定是否复用 Engine。

查看 `Build()` 方法 —— 它不创建 Engine，Engine 是外部传入的。所以 P2 的缓存需要调整设计：

**调整方案**：在 Builder 的 `Build()` 方法中，将当前 engine 与 cachedEngine 比较。若模板未变更且 cachedEngine 存在，则用 cachedEngine 替换当前 engine。

编辑 `internal/build/builder.go`，在 `Build()` 方法的增量/全量判断之后、页面收集之前，插入 Engine 缓存逻辑：

在 `b.skippedPaths = make(map[string]bool)` 之前添加：

```go
	// P2: reuse cached engine when templates haven't changed
	if !fullRebuild && b.cachedEngine != nil {
		b.engine = b.cachedEngine
	} else {
		b.cachedEngine = b.engine
	}
```

完整上下文（`Build()` 方法开头部分）：

```go
func (b *Builder) Build() error {
	fmt.Println("Chenhai 构建中...")

	var totalStart = time.Now()
	var t time.Time

	public := filepath.Join(b.root, "public")

	// Load build cache
	b.cache, _ = loadCache(b.root)
	defer b.cache.save()

	// Check if config or templates changed -> full rebuild
	configPath := filepath.Join(b.root, "config.yaml")
	configChanged := b.cache.configChanged(configPath)
	templatesChanged := b.hasTemplatesChanged()

	fullRebuild := configChanged || templatesChanged

	if fullRebuild {
		if configChanged {
			fmt.Println("  (配置文件已变更，全量重建)")
		} else {
			fmt.Println("  (模板文件已变更，全量重建)")
		}
		os.RemoveAll(public)
		b.cache.Files = make(map[string]string)
		b.cache.Config = ""
	} else {
		fmt.Println("  (增量构建模式)")
	}

	// P2: reuse cached engine when templates haven't changed
	if !fullRebuild && b.cachedEngine != nil {
		b.engine = b.cachedEngine
		fmt.Println("  (复用模板缓存)")
	} else {
		b.cachedEngine = b.engine
	}
```

- [ ] **Step 6: 构建并验证**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go build -o chenhai ./cmd/chenhai/
```

预期：编译成功。

```bash
cd /Users/kurong/Project/hekurong-site && ../Chenhai-hugo/chenhai build
```

连续运行两次 `chenhai build`：
- 第一次：显示「全量重建」（首次无缓存）
- 第二次：显示「增量构建模式」和「复用模板缓存」

- [ ] **Step 7: 运行全部测试**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/... -v
```

预期：全部 PASS

- [ ] **Step 8: Commit**

```bash
git add internal/build/builder.go internal/theme/engine.go
git commit -m "perf: template precompilation cache for incremental builds"
```

---

### Task 5: T1 — chenhai doctor 命令

**Files:**
- Create: `internal/cli/doctor.go`
- Create: `internal/cli/doctor_test.go`
- Modify: `internal/cli/root.go`

- [ ] **Step 1: 创建 doctor 命令文件**

新建 `internal/cli/doctor.go`：

```go
package cli

import (
	"fmt"
	"os"
	"path/filepath"
	"strings"

	"github.com/KurongTohsaka/chenhai-hugo/internal/config"
	"github.com/KurongTohsaka/chenhai-hugo/internal/content"
	"github.com/spf13/cobra"
)

var doctorCmd = &cobra.Command{
	Use:   "doctor",
	Short: "站点健康检查",
	Long:  "检测 config.yaml 语法、content/ 结构完整性、模板文件、缓存一致性等。",
	RunE: runDoctor,
}

func runDoctor(cmd *cobra.Command, args []string) error {
	root, _ := os.Getwd()
	fmt.Println("🔍 Chenhai Doctor — 站点健康检查")
	fmt.Println()

	errors := 0
	warnings := 0

	// 1. Check config.yaml exists
	configPath := filepath.Join(root, "config.yaml")
	if _, err := os.Stat(configPath); os.IsNotExist(err) {
		fmt.Println("✗ config.yaml 不存在")
		fmt.Println("  → 提示：在站点根目录创建 config.yaml 配置文件")
		fmt.Println()
		errors++
	} else {
		// 1b. Check config.yaml syntax
		_, err := config.Load(configPath)
		if err != nil {
			fmt.Printf("✗ config.yaml 解析失败：%v\n", err)
			fmt.Println()
			errors++
		} else {
			fmt.Println("✓ config.yaml 语法正确")
		}
	}

	// 2. Check content/ directory
	contentDir := filepath.Join(root, "content")
	if info, err := os.Stat(contentDir); os.IsNotExist(err) || !info.IsDir() {
		fmt.Println("✗ content/ 目录不存在")
		fmt.Println("  → 提示：创建 content/ 目录并放入 .md 文件")
		fmt.Println()
		errors++
	} else {
		// 2b. Walk all .md files and validate front matter
		var mdFiles []string
		filepath.Walk(contentDir, func(path string, info os.FileInfo, err error) error {
			if err != nil {
				return nil
			}
			if !info.IsDir() && strings.HasSuffix(path, ".md") {
				mdFiles = append(mdFiles, path)
			}
			return nil
		})

		if len(mdFiles) == 0 {
			fmt.Println("⚠ content/ 目录为空（没有 .md 文件）")
			warnings++
		} else {
			fmt.Printf("✓ content/ 目录存在（%d 个 .md 文件）\n", len(mdFiles))

			fmErrors := 0
			for _, path := range mdFiles {
				raw, err := os.ReadFile(path)
				if err != nil {
					fmt.Printf("✗ 无法读取 %s：%v\n", path, err)
					errors++
					continue
				}
				relPath, _ := filepath.Rel(root, path)
				_, _, err = content.ParseFrontMatter(raw)
				if err != nil {
					fmt.Printf("✗ %s\n  → Front Matter 解析失败：%v\n", relPath, err)
					fmErrors++
				}
			}
			if fmErrors > 0 {
				fmt.Printf("  共 %d 个文件 Front Matter 解析失败\n", fmErrors)
				errors += fmErrors
			}
		}
	}
	fmt.Println()

	// 3. Check required templates
	requiredTemplates := []string{"base.html", "single.html", "list.html", "index.html", "404.html"}
	missingTemplates := 0
	for _, name := range requiredTemplates {
		found := false
		for _, dir := range []string{
			filepath.Join(root, "layouts", name),
			filepath.Join(root, "themes", "zhenhai", "layouts", name),
		} {
			if _, err := os.Stat(dir); err == nil {
				found = true
				break
			}
		}
		if !found {
			// Check embedded theme — always available, just note if site overrides are missing
			// Zhenhai embedded templates are always available, so this check passes
			// unless user removed them (unlikely)
			fmt.Printf("⚠ 模板 %s 未在站点 layouts/ 或主题中找到（将使用嵌入模板）\n", name)
			warnings++
			missingTemplates++
		}
	}
	if missingTemplates == 0 {
		fmt.Println("✓ 所有必需模板存在")
	}
	fmt.Println()

	// 4. Check static/ directory
	staticDir := filepath.Join(root, "static")
	if info, err := os.Stat(staticDir); os.IsNotExist(err) || !info.IsDir() {
		fmt.Println("⚠ static/ 目录不存在（静态资源将跳过）")
		warnings++
	} else {
		fmt.Println("✓ static/ 目录存在")
	}
	fmt.Println()

	// 5. Check cache consistency
	cachePath := filepath.Join(root, ".chenhai-cache.json")
	if data, err := os.ReadFile(cachePath); err == nil {
		// Simple check: parse and verify referenced files exist
		// The cache format is JSON with a "files" map of path->hash
		_ = data // basic existence check; deeper validation would parse JSON
		fmt.Println("✓ .chenhai-cache.json 存在")
	} else {
		fmt.Println("⚠ .chenhai-cache.json 不存在（首次构建后自动生成）")
		warnings++
	}
	fmt.Println()

	// Summary
	if errors > 0 || warnings > 0 {
		fmt.Printf("✗ 发现 %d 个错误，%d 个警告\n", errors, warnings)
	} else {
		fmt.Println("✓ 站点健康检查通过，一切正常")
	}

	if errors > 0 {
		return fmt.Errorf("发现 %d 个错误", errors)
	}
	return nil
}
```

- [ ] **Step 2: 注册 doctor 命令**

编辑 `internal/cli/root.go`，在 `init()` 函数中添加：

```go
rootCmd.AddCommand(doctorCmd)
```

完整 init 函数：

```go
func init() {
	rootCmd.AddCommand(buildCmd)
	rootCmd.AddCommand(serveCmd)
	rootCmd.AddCommand(newCmd)
	rootCmd.AddCommand(cleanCmd)
	rootCmd.AddCommand(versionCmd)
	rootCmd.AddCommand(initCmd)
	rootCmd.AddCommand(deployCmd)
	rootCmd.AddCommand(doctorCmd)
	newCmd.AddCommand(themeCmd)
}
```

- [ ] **Step 3: 编译并测试 doctor 命令**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go build -o chenhai ./cmd/chenhai/
```

预期：编译成功。

```bash
cd /Users/kurong/Project/hekurong-site && ../Chenhai-hugo/chenhai doctor
```

预期：检测通过或报告实际问题。

- [ ] **Step 4: 编写 doctor 测试**

新建 `internal/cli/doctor_test.go`：

```go
package cli_test

import (
	"os"
	"path/filepath"
	"strings"
	"testing"

	"github.com/KurongTohsaka/chenhai-hugo/internal/cli"
)

func TestDoctor_NoConfigFile(t *testing.T) {
	root := t.TempDir()

	// Change to temp dir
	origDir, _ := os.Getwd()
	os.Chdir(root)
	defer os.Chdir(origDir)

	err := cli.ExecuteDoctor(root)
	if err == nil {
		t.Error("expected error for missing config.yaml")
	}
	if !strings.Contains(err.Error(), "config.yaml") {
		t.Errorf("error should mention config.yaml, got: %v", err)
	}
}

func TestDoctor_ValidSite(t *testing.T) {
	root := t.TempDir()

	// Create minimal valid site
	cfgYAML := []byte(`title: "Test Site"
baseURL: "https://example.com"
`)
	os.WriteFile(filepath.Join(root, "config.yaml"), cfgYAML, 0644)

	contentDir := filepath.Join(root, "content", "posts")
	os.MkdirAll(contentDir, 0755)
	postMD := []byte(`---
title: "Test Post"
date: "2024-01-15"
---
Body content.
`)
	os.WriteFile(filepath.Join(contentDir, "test.md"), postMD, 0644)

	origDir, _ := os.Getwd()
	os.Chdir(root)
	defer os.Chdir(origDir)

	err := cli.ExecuteDoctor(root)
	if err != nil {
		t.Errorf("unexpected error for valid site: %v", err)
	}
}

func TestDoctor_BadFrontMatter(t *testing.T) {
	root := t.TempDir()

	cfgYAML := []byte(`title: "Test Site"`)
	os.WriteFile(filepath.Join(root, "config.yaml"), cfgYAML, 0644)

	contentDir := filepath.Join(root, "content")
	os.MkdirAll(contentDir, 0755)
	badMD := []byte(`---
title: Broken
date: not-a-valid-date
---
Body.
`)
	os.WriteFile(filepath.Join(contentDir, "bad.md"), badMD, 0644)

	origDir, _ := os.Getwd()
	os.Chdir(root)
	defer os.Chdir(origDir)

	err := cli.ExecuteDoctor(root)
	if err == nil {
		t.Error("expected error for bad front matter")
	}
}
```

注：`ExecuteDoctor` 需要导出。修改 `internal/cli/doctor.go`，将 `runDoctor` 的核心逻辑提取为导出函数：

在 `doctor.go` 中，将 `runDoctor` 的实现提取到 `ExecuteDoctor` 函数：

```go
func runDoctor(cmd *cobra.Command, args []string) error {
	root, _ := os.Getwd()
	return ExecuteDoctor(root)
}

// ExecuteDoctor runs the site health check from the given root directory.
func ExecuteDoctor(root string) error {
	// ... (原 runDoctor 的全部逻辑，将 root 改为参数传入)
```

- [ ] **Step 5: 运行测试**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/cli/ -v -run "Doctor"
```

预期：全部 PASS

- [ ] **Step 6: 运行全部测试最终确认**

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo && go test ./internal/... -v
```

预期：全部 PASS

- [ ] **Step 7: 在本地博客站点最终验证**

```bash
cd /Users/kurong/Project/hekurong-site && ../Chenhai-hugo/chenhai doctor && ../Chenhai-hugo/chenhai build
```

预期：doctor 通过，构建成功，输出正常。

- [ ] **Step 8: Commit**

```bash
git add internal/cli/doctor.go internal/cli/doctor_test.go internal/cli/root.go
git commit -m "feat: add chenhai doctor command for site health checking"
```

---

## 总结

全部 5 个 Task 完成后，`go test ./internal/...` 应全部通过，`go build` 成功，本地博客构建正常。最终验证步骤：

```bash
cd /Users/kurong/Project/GoProject/Chenhai-hugo
go test ./internal/... -v
go build -o chenhai ./cmd/chenhai/
cd /Users/kurong/Project/hekurong-site
../Chenhai-hugo/chenhai doctor
../Chenhai-hugo/chenhai build
```
